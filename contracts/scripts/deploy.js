// Minimal placeholder: replace with your preferred deployer or @ton-community/blueprint.
// Here we just print instructions because deploying requires secrets.
console.log(`
Deploy instructions:
1) Build:  npm run build
2) Use your preferred deploy tool (blueprint/toncli) to deploy the compiled .tact artifact.
3) Set the deployed address into .env as ESCROW_ADDRESS and restart backend.
`);
